#!/bin/bash
# Path to the application
APP_PATH=<%= node[:spray][:path] =%>

cd ${APP_PATH}

case $1 in
    start)
        ${APP_PATH}/start
    ;;
    stop)
        ${APP_PATH}/stop
    ;;
    restart)
        ${APP_PATH}/stop
        ${APP_PATH}/start
    ;;
esac
exit 0